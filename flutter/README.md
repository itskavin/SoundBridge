# SoundBridge Flutter Workspace

This Flutter project is retained for future native and hybrid UI work.

Current v1 focus is web-first under [apps/web/public](../apps/web/public).

## Current status

- Flutter app compiles and can be used for experiments.
- Product-critical v1 features are currently being built in the web client first.

## Run Flutter web (optional)

1. Enter folder: `flutter`
2. Run: `flutter run -d chrome`

## Notes

- Keep Flutter changes modular so logic can be shared later.
- Do not block web-first release milestones on Flutter-specific work.
